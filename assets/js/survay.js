
import SuravyPage from "./SuravyPage"
export default class Suravy {
    constructor({pages}) {

        for (i = 0 ; i < n ; i ++ ) {
            this.page[i]= new SuravyPage(pages[i].name , [pages[1].questions] , )
        }
      }
}